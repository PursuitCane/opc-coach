# opc-sage · 需求文档

> OPC 军师 — 黑客松 demo。AI 商业顾问：读 BP、按红杉框架打分点评、对话完善、产出优化版 BP。
> Tag: `#shenicest-fission`

## 一、产品是什么

一句话：**OPC 把商业 BP 丢进来，军师像投资人一样给它打分、指出问题、陪它一起改，最后产出一版更好的 BP。**

面向：OPC（提交商业计划的人）。
场景：黑客松现场 demo，评委打开即用，无需登录。

## 二、核心流程（MVP）

```
① 提交 BP（上传 PDF）
      ↓  前端解析 PDF → 文本
② 军师评估 → 出报告（红杉六维度：打分 + 逐项改进建议）
      ↓
③ OPC 追问 / 补充（多轮对话）→ 完善 BP 内容
      ↓
④ 军师产出优化后的 BP（结构化文本，可复制/下载）
```

## 三、功能范围

### 做（demo 必须有）
1. **PDF 上传 + 解析**：前端解析 PDF 提取文本；解析失败给兜底提示（重传 / 手动贴文本）。
2. **评估报告**：按红杉 pitch 六维度评估——
   - 公司使命、痛点问题、产品方案、市场规模、竞争格局、团队与财务
   - 每维度：**分数 + 亮点 + 改进建议**
   - 附一个总体评分和一句话总评
3. **多轮追问**：OPC 就报告提问、补充信息，军师基于上下文继续回答、修正评估。
4. **产出优化 BP**：综合原 BP + 全部对话，生成一版优化后的 BP（结构化文本，可复制/下载）。

### 不做（demo 阶段）
- 登录 / 扫码引流（PM 的引流诉求延后，不挡主流程）
- 用户账号、历史记录持久化
- 多语言、移动端专门适配（够看即可）

## 四、技术方案

- **架构**：纯前端（无后端），前端直连 OpenAI。
- **栈（待定，倾向）**：React + Vite / Next.js（静态导出）。
- **PDF 解析**：`pdf.js`（浏览器端）。
- **AI**：OpenAI Responses API，结构化输出（六维度评分用固定 JSON schema，保证报告可渲染）。
- **部署**：静态托管（Vercel / Zeabur 静态站）。

### ⚠️ API Key 风险（必须先定）
纯前端直连会把 key 暴露在浏览器。demo 二选一：
- **A. 临时限额 key**：用一个设了额度上限的 key，demo 后作废。（省事，推荐）
- **B. 用户自填 key**：结果页/设置里让用户输入自己的 key，我方不掏钱不暴露。

## 五、Demo 打动评委的点

- 输入真实 BP，几秒出**投资人视角的结构化诊断**——直观、有说服力。
- 六维度雷达/评分卡，视觉冲击强。
- "陪你改" 的对话闭环 + 最终产出优化 BP，形成完整故事线。

## 六、待确认

- [ ] API Key 方案：A（临时 key）还是 B（用户自填）？
- [ ] 前端栈：Vite 还是 Next.js？
- [ ] 优化后 BP 的输出格式：纯文本 / Markdown / 分段结构化？
- [ ] 是否要雷达图等可视化（加分项，非必须）？
